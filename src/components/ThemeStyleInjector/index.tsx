/**
 * ThemeSettings im Admin steuern nur Standard-Theme (Hell/Dunkel/System).
 * Die eigentlichen Farben kommen aus globals.css (Deleyna Dark/Copper Design).
 * Kein Überschreiben der CSS-Variablen mehr – verhindert falsche Farben (z. B. Gelb).
 */
export async function ThemeStyleInjector() {
    return null
}
